Add gem 'rails', '~> 8.0.0'
If you have gem 'formtastic', '-~> 4.0.0' change it to 5 or remove the version.  
Run `bundle update`
Then run rails app:update

it will want to overwrite your bin/dev if you have it setup with foreman, so skip that one

